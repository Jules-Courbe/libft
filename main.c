#include <stdio.h>
#include "prototypes.h"

int main(void)
{
    char src[] = "Helloworld";
    char dest[] = "";
    ft_putstr("j'aimerais coder\n");
    printf("%d", (ft_strlen(src)));
    printf("\n%s", (ft_strcpy(dest, src)));
}